并发：
a)主要为了同时干多个事情，提高性能。
b)通过多个进程实现并发。（多进程并发）
c)通过一个进程创建多个线程。（多线程并发）优先考虑

进程：
a)使得可执行程序执行起来(.exe)。

线程：
a)线程是用于执行代码。
b)线程相当于一个通路，一条路是一条线程。
c)一个进程自动包含一个主线程，进程运行主线程就运行，也可以通过编码创建多个非主线程，不建议太多，按实际调整。
d)主线程随着进程自动启动，一个进程中最少有一个进程。
e)多线程程序可以同时做多件事，提高运行效率，但不是越多越好。

多线程并发：
方法1：
    a)包含一个头文件thread要包含进来。include <thread>
    b)初始函数要写。自己创建的线程要从初始函数开始。
    c)main中写代码。
    d)过程：thread myobj(myprint)//创建一个线程，并执行myprint
        myobj.join()//阻塞主线程，让主线程等待子线程，然后执行主线程。
        detach()：分离，主线程与子线程回合，各自执行，不受自我控制。一旦用detach就不能用join。
方法2：
    使用类
